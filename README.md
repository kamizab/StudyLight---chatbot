# StudyLight---chatbot
StudyLight is a Streamlit-based quiz generator powered by an LLM (OpenAI or Gemini) that creates interactive multiple-choice quizzes through a conversational interface. The application securely manages API keys using Streamlit secrets and validates structured JSON responses to ensure reliable quiz generation and grading.
